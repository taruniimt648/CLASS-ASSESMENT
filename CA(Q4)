#include <iostream>
#include <string>

using namespace std;

int main() {
   int n;
   cin >> n;

   string s = "1";

   for (int i = 0; i < n; i++) {
       cout << s << endl;
       if (i % 2 == 0) {
           s = "0" + s;
       } else {
           s = "1" + s;
       }
   }

   return 0;
}
